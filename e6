#!/bin/bash

if [[ "$OSTYPE" == "linux-gnu"* ]]; then
    # ...
    /usr/bin/emacs -q -l ./mini.el --debug-init
elif [[ "$OSTYPE" == "darwin"* ]]; then
    # Mac OSX
    /Applications/MacPorts/Emacs.app/Contents/MacOS/Emacs -q -l ./mini.el --debug-init
fi



