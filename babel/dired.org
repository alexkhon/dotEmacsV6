* nerd-icons
#+begin_src emacs-lisp
(use-package nerd-icons-dired
  ;; standardize the icons for dired
  :hook
  (dired-mode . nerd-icons-dired-mode)
  )
#+end_src


* dired preview
https://github.com/protesilaos/dired-preview
https://protesilaos.com/emacs/dired-preview
#+begin_src emacs-lisp
(use-package dired-preview
  :straight
  (:host github :repo "protesilaos/dired-preview" :branch "main" :files ("*.el" "texi"))
  :config
  (setq dired-preview-global-mode t)
  )
#+end_src

* dired-sort
- There is something broken with this package
- directory .. is displayed and in the wrong location
#+begin_src emacs-lisp
;; (use-package dired-sort
;;   :straight
;;   (:host github :repo "MichaelKazarian/dired-sort" :branch "master" :files ("*.el" "texi"))
;;   :ensure t  ; Set to t if installed via a package repository
;;   :config
;;   (dired-sort-mode 1)
;;   )
#+end_src

* dired-x (built into emacs)
#+begin_src emacs-lisp
(require 'dired-x)
;;(add-hook 'dired-mode-hook 'dired-omit-mode)
;;(setq dired-omit-files (concat dired-omit-files "\\|^\\.\\.?$"))

#+end_src

* dired-subtree
- doesn't seem to work either
#+begin_src emacs-lisp
;; (use-package dired-subtree
;;   :ensure t
;;   :after dired
;;   )
#+end_src

* dired mode configs (https://www.youtube.com/watch?v=L_4pLN0gXGI)
** notes
If you dired with two panes, emacs will know to copy or move to the
second pane
** code
#+begin_src emacs-lisp
  (setq delete-by-moving-to-trash t)
  (setq dired-dwim-target t)
  (setq dired-kill-when-opening-new-dired-buffer t)            ;; keep dired in one buffer
  (setq dired-listing-switches "--group-directories-first")    ;; group directories first
  (add-hook 'dired-mode-hook 'dired-hide-details-mode)
#+end_src
